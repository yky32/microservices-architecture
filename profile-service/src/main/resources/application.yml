eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka

server:
    port: 8082

spring:
    application:
        name: PROFILE-SERVICE
    datasource:
        url: jdbc:postgresql://yky32.asuscomm.com:5435/tgt-profile
        username: postgres
        password: postgres
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
